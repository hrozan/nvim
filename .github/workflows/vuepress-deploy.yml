name: Deploy Docs
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - name: Luacheck linter
        uses: lunarmodules/luacheck@v0
        with:
          args: lua/
